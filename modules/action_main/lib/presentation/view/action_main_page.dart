import 'package:flutter/widgets.dart';

class ActionMainPage extends StatelessWidget {
  const ActionMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
